/**
 * Compatibility cleanup for experimental FEATURE-057 builds.
 *
 * Contextual backgrounds are now rendered natively by GameApp and
 * storyPresentation. This module intentionally has no runtime side effects.
 */
export {};
